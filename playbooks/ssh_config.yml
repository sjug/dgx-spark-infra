---
- name: Enforce captured SSH server configuration on DGX Spark nodes
  hosts: "{{ target | default('cleanup_targets') }}"
  become: true
  tasks:
    - name: Apply captured sshd drop-in configuration
      ansible.builtin.import_role:
        name: dgx_spark_sync
        tasks_from: config_ssh
