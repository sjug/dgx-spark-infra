# Local convenience overrides for private infrastructure.
# Copy to .env.mk and adjust as needed.

INVENTORY := inventory/hosts.yml
SOURCE_HOST := source-node
DIFF_HOST_A := source-node
DIFF_HOST_B := target-node
MANAGED_USER := admin
